// var history = chrome.storage.local.get("history");
// if (history == undefined) chrome.storage.local.set({
// 	history: {}
// })

// var current_video;

// chrome.runtime.onMessage.addListener(
// 	function (request, _sender, sendResponse) {

// 		if (request == "getState") {
// 			sendResponse(history);
// 		}
// 		else {
			
// 		}
// 	}
// );
